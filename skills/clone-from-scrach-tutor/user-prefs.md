# 用户偏好记录（clone-from-scratch-tutor）

## 技术背景（2026-09-19 用户更正）
- **主力栈：TypeScript / Node.js**（做过服务端开发）。类比映射、词汇表、背景卡片一律用 TS/Node 生态的概念：如 select! → `Promise.race`，tokio 运行时 → Node 事件循环（注意多线程差异），crate → npm package，trait → interface。
- 先前按 "Python/后端为主" 做过映射，已作废。
- **明确不熟悉**：嵌入式 / 硬件控制、模型推理部署、音频 / 流式处理——首次用到时插背景卡片。

## 流程偏好
- 里程碑的代码实现交由 subagent 构建，主 agent 保留设计、验收与讲解。
